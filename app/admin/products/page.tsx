import Image from 'next/image'
import { Plus, MoreHorizontal } from 'lucide-react'
import { mockProducts } from '@/data/mockData'
export default function AdminProductsPage() { return <div><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Catalog</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Products</h1><p className="mt-2 text-slate-600">Manage your published product catalog.</p></div><button className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Plus className="h-4 w-4" /> Add product</button></div><div className="mt-8 overflow-hidden rounded-md border border-border bg-card"><div className="divide-y divide-border">{mockProducts.map(product => <div key={product.id} className="flex flex-wrap items-center gap-4 p-4 sm:p-5"><div className="relative h-14 w-14 overflow-hidden rounded-md bg-muted"><Image src={product.images[0]} alt="" fill className="object-cover" sizes="56px" /></div><div className="min-w-40 flex-1"><p className="font-semibold text-slate-950">{product.name}</p><p className="text-sm text-slate-500">{product.category_tags.join(' · ')}</p></div><span className="text-sm font-semibold text-slate-700">${product.price.toFixed(2)}</span><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">Published</span><button aria-label={`Actions for ${product.name}`} className="rounded-md p-2 text-slate-400 hover:bg-muted hover:text-slate-950"><MoreHorizontal className="h-5 w-5" /></button></div>)}</div></div></div> }
