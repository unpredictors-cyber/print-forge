import Link from 'next/link'
import { Plus, FileText } from 'lucide-react'
import { mockBlogPosts } from '@/data/mockData'
export default function AdminBlogPage() { return <div><div className="flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Content</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Blog CMS</h1><p className="mt-2 text-slate-600">Publish practical notes from the workshop.</p></div><Link href="/admin/blog/create" className="inline-flex items-center gap-2 rounded-md bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Plus className="h-4 w-4" /> New post</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2">{mockBlogPosts.map(post => <div key={post.id} className="rounded-md border border-border bg-card p-5"><FileText className="h-5 w-5 text-primary" /><h2 className="mt-4 font-bold text-slate-950">{post.title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{post.excerpt}</p><p className="mt-4 text-xs font-semibold uppercase tracking-wider text-slate-400">Published · {post.read_time} min read</p></div>)}</div></div> }
