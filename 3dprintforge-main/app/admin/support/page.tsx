import { mockTickets } from '@/data/mockData'
export default function AdminSupportPage() { return <div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Customer care</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Support tickets</h1><p className="mt-2 text-slate-600">Keep every customer request moving.</p><div className="mt-8 overflow-hidden rounded-md border border-border bg-card"><div className="divide-y divide-border">{mockTickets.map(ticket => <div key={ticket.id} className="flex flex-wrap items-center justify-between gap-4 p-5"><div><p className="font-semibold text-slate-950">{ticket.id} · {ticket.type}</p><p className="mt-1 text-sm text-slate-500">Order {ticket.order_id} · Last updated {ticket.created_at}</p></div><div className="flex items-center gap-3"><span className="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">{ticket.status}</span><button className="rounded-md bg-slate-950 px-3 py-2 text-sm font-semibold text-white">Open thread</button></div></div>)}</div></div></div> }
