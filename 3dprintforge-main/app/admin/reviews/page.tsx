import { mockReviews, mockProducts } from '@/data/mockData'
export default function AdminReviewsPage() { return <div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Moderation</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Reviews</h1><p className="mt-2 text-slate-600">Review customer feedback before it reaches the storefront.</p><div className="mt-8 space-y-4">{mockReviews.map(review => <div key={review.id} className="rounded-md border border-border bg-card p-5"><div className="flex flex-wrap justify-between gap-3"><div><p className="font-semibold text-slate-950">{review.user_name} · {mockProducts.find(p=>p.id===review.product_id)?.name}</p><p className="text-sm text-amber-600">{'★'.repeat(review.rating)}{'☆'.repeat(5-review.rating)}</p></div><button className="rounded-md border border-border px-3 py-1.5 text-sm font-semibold text-slate-600 hover:bg-red-50 hover:text-red-700">Remove</button></div><p className="mt-3 text-slate-600">{review.comment}</p></div>)}</div></div> }
