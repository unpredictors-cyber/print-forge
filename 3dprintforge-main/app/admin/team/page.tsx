import { mockProfiles } from '@/data/mockData'
export default function AdminTeamPage() { return <div><div className="flex items-end justify-between"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Access control</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Team & scopes</h1><p className="mt-2 text-slate-600">Manage operational access for your team.</p></div><button className="rounded-md bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Invite teammate</button></div><div className="mt-8 overflow-hidden rounded-md border border-border bg-card"><div className="divide-y divide-border">{mockProfiles.map(profile => <div key={profile.id} className="flex flex-wrap items-center justify-between gap-4 p-5"><div><p className="font-semibold text-slate-950">{profile.email}</p><p className="text-sm text-slate-500">{profile.role}</p></div><div className="flex flex-wrap gap-2">{profile.scopes.map(scope => <span key={scope} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-600">{scope}</span>)}</div></div>)}</div></div></div> }
